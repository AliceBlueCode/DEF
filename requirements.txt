# DEF(kari) - Python dependencies
# Install: pip install -r requirements.txt
#
# NOTE: keep this file pure ASCII. pip decodes requirements files with the
# system locale when there is no BOM, so non-ASCII comments break
# "pip install -r requirements.txt" on Japanese Windows (cp932) and other
# non-UTF-8 locales.

# Web framework
# fastapi >= 0.96 pulls in pydantic v2 / starlette 1.x, which conflicts with
# the pydantic v1 style (@validator etc.) the codebase is written against,
# so the upper bound is pinned.
fastapi>=0.95.0,<0.96.0
uvicorn[standard]>=0.52.1
python-multipart>=0.0.32

# Authentication (JWT: online-session invite codes, host/player tokens)
python-jose>=3.5.0

# HTTP client
requests>=2.34.2

# Image processing
Pillow>=12.3.0

# Encryption
cryptography>=50.0.0

# Validation
jsonschema>=4.26.0

# Config / translation tool
PyYAML>=6.0.3

# Translation (local translation provider)
deep-translator>=1.11.4

# --- dev / test ---
pytest>=9.1.1
pytest-asyncio>=1.4.0
# For FastAPI TestClient. Newer httpx/anyio change TestClient initialization
# and exception propagation when combined with the starlette that
# fastapi<0.96 requires, so both are pinned.
httpx==0.23.3
anyio<4.0.0
